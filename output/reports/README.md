# output/reports

No longer where generated tests live — that moved to `output/Unit Tests/<GradeFolder>/<SubjectFolder>/` (see its own README).

This folder now only holds miscellaneous source "system instructions" documents that subject-specific `test-generator/formats/*.md` override files were distilled from (e.g. `Maths_instructions.md`, `Science_instructions.md`, `socialstudies_instruction.md`, `English_intruction.md`). Not committed to git — only this README is tracked.
